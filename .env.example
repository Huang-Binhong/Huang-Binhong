# 服务端口
PORT=8080

# 数据目录（存放 SQLite 数据库）
DATA_PATH=./data

# 数据库文件路径（容器内绝对路径）
DATABASE_PATH=/app/data/myapp.db

# CORS 跨域配置（可选）
# 允许的来源，默认为 * (所有来源)
# 生产环境建议设置具体域名，如：https://example.com
CORS_ALLOW_ORIGIN=*

# Cloudflare Tunnel Token（可选，用于内网穿透）
# 获取方式：https://dash.cloudflare.com/ -> Zero Trust -> Tunnels
CLOUDFLARE_TUNNEL_TOKEN=
